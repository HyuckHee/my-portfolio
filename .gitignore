# dependencies
node_modules/

# build output (Vercel이 자체 빌드하므로 커밋 불필요)
dist/

# env — 로컬 시크릿, 절대 커밋 금지
.env
.env.local
.env.*.local

# tooling state
.gstack/
.vercel/

# Claude Code 로컬 권한 설정 — 머신별 세션 상태라 공유 불필요
.claude/settings.local.json

# OS
.DS_Store
