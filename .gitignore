# dependencies
node_modules/

# build output (Vercel이 자체 빌드하므로 커밋 불필요)
dist/

# 앱인토스 미니앱 빌드 산출물 (appintoss/ 는 자체 패키지 — dist/ · node_modules/ 는 위 규칙이 커버)
.granite/
*.ait

# env — 로컬 시크릿, 절대 커밋 금지
.env
.env.local
.env.*.local

# tooling state
.gstack/
.vercel/

# Claude Code 로컬 권한 설정 — 머신별 세션 상태라 공유 불필요
.claude/settings.local.json

# OS
.DS_Store
